[project]
name = "dutch-law-bindu-agent"
version = "0.1.0"
description = "Dutch-Law-MCP served as a Bindu agent (Agno + MCP + Bindu + DID)"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "bindu>=2026.20.3",
    "agno>=1.7.0",
    "openai>=1.50.0",
    "mcp>=1.10.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
